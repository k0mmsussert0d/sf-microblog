import React, {ReactElement} from 'react';
import AddPostForm from '../components/add/AddPostForm';

const AddPost: React.FC = (): ReactElement => {

  return (
    <>
      <AddPostForm />
    </>
  );
};

export default AddPost;
